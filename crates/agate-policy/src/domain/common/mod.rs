//! Base building blocks (DDD seedwork) for the policy context's domain model.
//!
//! Kept per-crate on purpose — there is no cross-context shared kernel.

pub mod errors;
pub mod services;
pub mod values;
