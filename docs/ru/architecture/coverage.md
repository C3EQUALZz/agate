# Покрытие и ограничения

Эта страница честно описывает, **что Agate инспектирует сегодня**, а что
пересылает без инспекции — чтобы вы разворачивали его, не переоценивая защиту.
[Модель угроз](threat-model.md) описывает *целевую* модель безопасности; эта
страница — честное *текущее* состояние. Полный анализ и дорожная карта по
закрытию пробелов находятся в репозитории:
[`docs/design/security-coverage-roadmap.md`](https://github.com/C3EQUALZz/agate/blob/main/docs/design/security-coverage-roadmap.md).

!!! warning "Прочитайте это, прежде чем полагаться на Agate как на единственный барьер"
    Сейчас Agate обеспечивает авторизацию вызовов инструментов (по **имени** и
    по правилам запрета **аргументов**), редакцию секретов в исходящем тексте,
    результатах инструментов и состоянии, а также fail-closed для битых событий.
    Часть угроз из модели угроз пока не реализована (ниже) — в частности
    валидация JSON Patch (RFC 6902). До выполнения оставшихся пунктов дорожной
    карты считайте Agate одним из слоёв защиты, а не полным «межсетевым экраном»
    для агента.

## Что инспектируется

| Событие / ввод | Что происходит | Может ли политика вмешаться? |
|---|---|---|
| `TOOL_CALL_*` (собранный) | буферизуется в полный вызов инструмента, затем оценивается; аргументы проверяются на SSRF-URL | **Да** — разрешить/запретить по **имени** и по правилам запрета **аргументов** (`[[policy.tools.deny_arguments]]`); SSRF-URL в аргументах отбрасывает вызов |
| `TEXT_MESSAGE_CONTENT` | сканируется на шаблоны секретов и SSRF-URL | **Да** — редактировать (литерал или regex); SSRF-URL отбрасывает фрагмент |
| `TOOL_CALL_RESULT` | содержимое результата сканируется на шаблоны секретов и SSRF-URL | **Да** — редактировать (литерал или regex; поверхность косвенной инъекции / эксфильтрации); SSRF-URL отбрасывает результат |
| `STATE_SNAPSHOT` / `STATE_DELTA` | бюджет размера/числа операций **и** сканирование payload на маркеры секретов | **Да** — запрет при совпадении (структурный payload нельзя замаскировать, поэтому он блокируется, а не утекает) |
| Жизненный цикл (`RUN_*`, `STEP_*`) | порядок проверяется конечным автоматом `Run` | только структурно (без вердикта политики) |
| «Нога» запроса (`RunAgentInput`) | авторизуется `tools[*].name`; текст `user`-сообщений проверяется на SSRF-URL — доменный хост **резолвится**, и его адреса перепроверяются, закрывая DNS-rebinding | **Да** — отклонить до пересылки |
| Битые **известные** события | распознанный тип с отсутствующим/пустым обязательным полем нельзя инспектировать → обрабатывается по `[policy].on_malformed_event` (по умолчанию `terminate`) | **Да** — по умолчанию fail-closed |

## Что пересылается без инспекции

| Событие / ввод | Почему это важно |
|---|---|
| Операции JSON Patch (RFC 6902) в **состоянии** | payload сканируется на маркеры секретов, но сами операции JSON Patch не валидируются/не ограничиваются против отравления |
| `RAW`, `CUSTOM`, `REASONING_ENCRYPTED_VALUE` | непрозрачные — пересылаются как есть, не инспектируются |
| Неизвестные / будущие типы событий AG-UI | пересылаются сырыми |
| Скрытые поля запроса (`system`, `forwardedProps`, `context`, входящий `state`) | не извлекаются, поэтому инъекция в них не проверяется |

## Операционные ограничения

- **Аутентификация** выключена по умолчанию (открытый прокси). Задайте
  `[proxy].api_keys` или поставьте перед Agate шлюз. См.
  [Конфигурацию](../getting-started/configuration.md).
- **Бюджеты против DoS** сегодня: глобальный лимит конкурентности, лимит размера
  тела запроса, таймауты подключения/чтения, **бюджет ответа на прогон**
  (`max_response_events` / `max_response_bytes`), отрезающий зарвавшегося агента,
  и опциональный **rate-лимит по IP клиента** (`rate_limit_per_second` /
  `rate_limit_burst`, по умолчанию выключен), отбрасывающий потоки с `429`. IP
  берётся из пира соединения, поэтому он осмыслен только там, где Agate видит
  реального клиента.
- **Полнота аудита:** записи ставятся в очередь ограниченного outbox; при
  устойчивом противодавлении запись может быть отброшена (с логом и счётчиком)
  без остановки плоскости данных — поэтому переполненный outbox может оставить
  пробел в журнале прозрачности. Следите за метрикой отбрасывания аудита.
- **TLS** терминируется на прокси (необходимо для инспекции открытого текста);
  по умолчанию выключен, настраивается в секции `[tls]`.

## Дорожная карта

Закрытие оставшихся пробелов упорядочено в
[`security-coverage-roadmap.md`](https://github.com/C3EQUALZz/agate/blob/main/docs/design/security-coverage-roadmap.md):
валидация JSON Patch (RFC 6902).
(Fail-closed для битых событий, правила запрета аргументов,
редакция секретов в результатах и состоянии, бюджеты ответа на прогон, а также
SSRF-скрин на обеих «ногах» — с резолвом DNS против rebinding — уже
реализованы.)
